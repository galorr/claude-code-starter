Invoke the tdd-guide agent to implement the requested feature using test-driven development.

Enforce the RED → GREEN → REFACTOR cycle strictly:

1. **Scaffold interfaces** — Define types and data structures first
2. **Write failing tests** — Tests MUST be written before any implementation
3. **Run tests — verify they FAIL** — Never skip this step
4. **Write minimal implementation** — Only enough code to pass the tests
5. **Run tests — verify they PASS**
6. **Refactor** — Improve code while keeping tests green
7. **Verify coverage** — Must be 80%+ (100% for auth/payments/security)

Use: $ARGUMENTS
