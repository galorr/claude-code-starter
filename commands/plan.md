Invoke the planner agent to create a detailed implementation plan before writing any code.

Follow this process:
1. **Restate requirements** — Clarify exactly what needs to be built
2. **Identify risks** — Surface blockers, dependencies, edge cases
3. **Create step plan** — Break into phases with specific file paths and actions
4. **WAIT for confirmation** — Do NOT write any code until the user explicitly approves

The plan must include:
- Implementation steps with exact file paths
- Testing strategy (unit, integration, E2E)
- Risks and mitigations
- Success criteria

**Do not write a single line of implementation code until the user confirms the plan.**

Task: $ARGUMENTS
