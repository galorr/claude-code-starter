---
name: tc
description: Shortcut for trycycle. Invoke trycycle only when the user requests it by name.
---

# tc (trycycle shortcut)

This is a shortcut alias for the trycycle skill. When invoked, immediately invoke the `trycycle` skill instead, passing along any arguments the user provided.
